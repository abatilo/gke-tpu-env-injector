name: "Integration tests"
on:
  pull_request: {}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: jdxcode/rtx-action@v1
      - name: Deploy for CI
        run: |
          make ci
          kubectl port-forward svc/gke-tpu-env-injector 8000:80 &
          sleep 5
          curl http://localhost:8000/
